apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_lab.fullname" . }}
  labels:
    {{- include "helm_lab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_lab.selectorLabels" . | nindent 4 }}
